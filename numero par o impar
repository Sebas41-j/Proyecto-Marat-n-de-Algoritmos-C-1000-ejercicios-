#include <iostream>
int main() {
    int numero;
    std::cout << "Ingrese un número: ";
    std::cin >> numero;

    if (numero % 2 == 0) {
        std::cout << "El número es PAR." << std::endl;
    } else {
        std::cout << "El número es IMPAR." << std::endl;
    }

    return 0;
}
